# RP-JUP-EXECUTIONER — Richard Patterson (@De-ASI-INTERFACE)
# UNIQUE CODE IDENTIFIER: RP-DEASI-JUP-2026-0619-001

# ── Solana RPC ────────────────────────────────────────────────────────────────
SOLANA_RPC_MAINNET=https://mainnet.helius-rpc.com/?api-key=YOUR_KEY
SOLANA_RPC_ALCHEMY=https://solana-mainnet.g.alchemy.com/v2/YOUR_KEY
JITO_RPC=https://mainnet.block-engine.jito.wtf

# ── Execution Keypair ─────────────────────────────────────────────────────────
PRIVATE_KEY=YOUR_BASE58_PRIVATE_KEY
DEPLOYER_WALLET=CuAjiyp7Rfj4vvjQ8JWVMLeXYYumaTYKpZf9oWs2A4my

# ── Jupiter ───────────────────────────────────────────────────────────────────
JUP_QUOTE_API=https://quote-api.jup.ag/v6
PORT=4002
FEE_BPS=20

# ── x402 Payment Gate ─────────────────────────────────────────────────────────
# USDC ATA that receives execution fees (DEASI vault)
X402_VAULT_USDC_ACCOUNT=YOUR_VAULT_USDC_ATA

# USDC SPL token mint (mainnet)
X402_USDC_MINT=EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v

# Per-route fees in atomic USDC (1 USDC = 1_000_000)
# Quote: $0.01 | Swap: $0.05
X402_QUOTE_FEE_USDC=10000
X402_SWAP_FEE_USDC=50000

# ── x402 Session Tokens (HFT bypass) ─────────────────────────────────────────
# Set to 'true' to enable session token support for HFT workloads
X402_ACCEPT_SESSION_TOKENS=false

# HMAC secret for session token signing (min 32 chars, keep secret)
X402_SESSION_SECRET=YOUR_SESSION_SECRET_MIN_32_CHARS

# Default session TTL in seconds (e.g. 3600 = 1 hour)
X402_SESSION_TTL_SECONDS=3600

# ── Rate Limiting ─────────────────────────────────────────────────────────────
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=120
